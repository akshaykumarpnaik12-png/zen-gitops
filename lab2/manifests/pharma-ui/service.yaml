apiVersion: v1
kind: Service
metadata:
  name: pharma-ui
  namespace: dev
  labels:
    app: pharma-ui
    env: dev
spec:
  type: ClusterIP
  selector:
    app: pharma-ui
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
